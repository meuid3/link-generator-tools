import { ColCenter, ColEnd, ColSpaceAround, ColSpaceBetween, ColStart, RowCenter, RowEnd, RowSpaceAround, RowSpaceBetween, RowStart, Container } from "./Grid";

export const Grid = {
  RowCenter: RowCenter,
  RowStart: RowStart,
  RowEnd: RowEnd,
  ColStart: ColStart,
  ColCenter: ColCenter,
  ColEnd: ColEnd,
  RowSpaceBetween: RowSpaceBetween,
  ColSpaceBetween: ColSpaceBetween,
  RowSpaceAround: RowSpaceAround,
  ColSpaceAround: ColSpaceAround,
  Container: Container
}